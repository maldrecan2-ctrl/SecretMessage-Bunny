{
  "name": "bunny-SecretMessage",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/index.ts --bundle --outfile=index.js --platform=browser --target=es2017 --minify --external:@vendetta --external:@vendetta/*"
  },
  "devDependencies": {
    "esbuild": "^0.19.0",
    "typescript": "^5.0.0"
  }
}
